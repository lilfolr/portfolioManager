import { View } from 'react-native';

import { Sans } from '@/src/ui/text';

// Placeholder until the holding detail screen lands.
export default function HoldingDetailScreen() {
  return (
    <View className="flex-1 items-center justify-center">
      <Sans testID="holdingDetailPlaceholder">Holding detail</Sans>
    </View>
  );
}
